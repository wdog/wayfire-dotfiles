#!/bin/bash
cd ~/repo/dotfiles-wayfire

# Check if arguments provided
if [ $# -eq 0 ]; then
    # No arguments, run curses interface
    python3 dotfiles_curses.py
else
    # Arguments provided, use CLI version
    python3 dotfiles_manager.py "$@"
fi